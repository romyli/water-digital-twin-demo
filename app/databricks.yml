bundle:
  name: water-digital-twin-demo

variables:
  genie_space_id:
    description: Default Genie Space ID
  genie_space_name:
    description: Genie Space for AI-powered data queries. Space IDs configured via plugin config.
  postgres_branch:
    description: Full Lakebase Postgres branch resource name. Obtain by running `databricks postgres list-branches projects/{project-id}`, select the desired item from the output array and use its .name value.
  postgres_database:
    description: Full Lakebase Postgres database resource name. Obtain by running `databricks postgres list-databases {branch-name}`, select the desired item from the output array and use its .name value. Requires the branch resource name.

resources:
  apps:
    app:
      name: "water-digital-twin-demo"
      description: "Water Utilities Digital Twin"
      source_code_path: ./
      user_api_scopes:
        - dashboards.genie

      # The resources which this app has access to.
      resources:
        - name: genie-space
          genie_space:
            name: ${var.genie_space_name}
            space_id: ${var.genie_space_id}
            permission: CAN_RUN
        - name: postgres
          postgres:
            branch: ${var.postgres_branch}
            database: ${var.postgres_database}
            permission: CAN_CONNECT_AND_CREATE

targets:
  default:
    default: true
    workspace:
      host: https://adb-984752964297111.11.azuredatabricks.net

    variables:
      genie_space_id: "00000000000000000000000000000000"
      genie_space_name: "Network Operations"
      postgres_branch: projects/water-digital-twin-demo-demo/branches/production
      postgres_database: projects/water-digital-twin-demo-demo/branches/production/databases/db-osgm-kmix6wutja
