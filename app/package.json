{
  "name": "water-digital-twin",
  "version": "1.0.0",
  "main": "dist/server.js",
  "type": "module",
  "description": "Water Utilities Digital Twin — Databricks AppKit",
  "scripts": {
    "start": "NODE_ENV=production node --env-file-if-exists=./.env ./dist/server.js",
    "dev": "NODE_ENV=development tsx watch --tsconfig ./tsconfig.server.json --env-file-if-exists=./.env ./server/server.ts",
    "build:client": "tsc -b tsconfig.client.json && vite build --config client/vite.config.ts",
    "build:server": "tsdown -c tsdown.server.config.ts",
    "build": "npm run build:server && npm run build:client",
    "typecheck": "tsc -p ./tsconfig.server.json --noEmit && tsc -p ./tsconfig.client.json --noEmit",
    "clean": "rm -rf client/dist dist build node_modules",
    "postinstall": "npm run typegen || true",
    "prebuild": "npm run sync || true",
    "predev": "npm run sync || true",
    "sync": "appkit plugin sync --write --silent",
    "typegen": "appkit generate-types"
  },
  "dependencies": {
    "@databricks/appkit": "0.20.3",
    "@databricks/appkit-ui": "0.20.3",
    "@tanstack/react-query": "^5.12.0",
    "date-fns": "^3.0.0",
    "maplibre-gl": "^4.7.0",
    "react": "^19.1.1",
    "react-dom": "^19.1.1",
    "react-router": "^7.13.0",
    "react-router-dom": "^6.20.0",
    "recharts": "^2.10.0"
  },
  "devDependencies": {
    "@tailwindcss/postcss": "^4.1.17",
    "@tailwindcss/vite": "^4.1.17",

    "@types/node": "^24.6.0",
    "@types/react": "^19.1.16",
    "@types/react-dom": "^19.1.9",
    "@vitejs/plugin-react": "^5.0.4",
    "autoprefixer": "^10.4.16",
    "tailwindcss": "^4.0.14",
    "tsdown": "^0.20.3",
    "tsx": "^4.20.6",
    "typescript": "~5.9.3",
    "vite": "npm:rolldown-vite@7.1.14"
  },
  "overrides": {
    "vite": "npm:rolldown-vite@7.1.14"
  }
}
